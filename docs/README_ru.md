# 🚀 WhyCrash
**WhyCrash** — это суперавтоматический ИИ-помощник для обработки ошибок в Python. Когда ваш код падает, WhyCrash перехватывает ошибку, анализирует её с помощью нейросетей (OpenRouter + Minimax), собирает контекст из ваших локальных файлов и выдает причину с возможностью **АВТОМАТИЧЕСКОГО ИСПРАВЛЕНИЯ КОДА**.

Ваш код упал? Нейросеть объяснит почему и сама заменит сломанный файл на исправленный (если вы разрешите).

![GitHub license](https://img.shields.io/badge/license-MIT-blue.svg)
![Python Versions](https://img.shields.io/badge/python-3.8%2B-blue)

## ✨ Главные возможности
- 🧠 **Умный анализ Traceback**: Понимает не только строку с ошибкой, но и собирает импортированные локальные файлы проекта.
- 🛠️ **Авто-починка**: Предлагает готовое исправление и умеет сама переписывать целевые файлы Python.
- 🎯 **Точечный контроль**: Вы сами решаете, где ловить ошибки: во всем проекте целиком, в одной функции или же в конкретном блоке кода.
- 🎨 **Красивый интерфейс**: Использует библиотеку `rich` для красивых окон и форматирования в терминале.

---

## 📦 Установка

```bash
pip install WhyCrash
```
> *(Требуется установленный `requests`, `rich` и `questionary` — они установятся автоматически)*

---

## 🛠️ Как использовать

У вас есть 4 способа контролировать, какие ошибки должен отлавливать WhyCrash. Выберите тот, который подходит больше всего!

### 1. Глобальный перехват (самый простой)
Если вы хотите, чтобы **любая** необработанная ошибка в вашей программе анализировалась нейросетью:

```python
import WhyCrash

# Включаем отлов ошибок для всего скрипта
WhyCrash.debug()

# Если дальше код упадет, WhyCrash придет на помощь!
print(1 / 0)
```

### 2. Динамическое включение/выключение (start & end)
Если у вас есть большой блок кода, и вы хотите включить умный анализ прямо перед ним, а после — выключить:

```python
import WhyCrash

# ... обычный код без WhyCrash ...

WhyCrash.start_debug()  # Включаем перехватчик

a = "текст"
b = int(a)  # <-- Эта ошибка пойдет в нейросеть!

WhyCrash.end_debug()    # Выключаем перехватчик (возвращаем стандартное поведение)
```

### 3. Декоратор для отдельных функций `@catch_errors`
Если вас беспокоит надежность работы только конкретной функции, вы можете обернуть её в декоратор. Если функция упадет, сработает WhyCrash, а системные ошибки вне её останутся нетронутыми.

```python
from WhyCrash import catch_errors

@catch_errors
def my_danger_function():
    # Если сломается здесь — сработает WhyCrash
    file = open("no_exist.txt", "r")

def normal_function():
    # А если сломается здесь — будет обычный traceback Python
    pass

my_danger_function()
```

### 4. Контекстный менеджер `with catch_block()`
Для максимального точечного контроля, если вы ожидаете сбой буквально в 2-х конкретных строчках кода:

```python
from WhyCrash import catch_block

print("Начинаем работу...")
text = "100"

with catch_block():
    # Только этот код внутри блока мониторится 
    number = int(text)
    result = number / 0  # Приведет к ошибке, которая будет отправлена в WhyCrash!

print("Этот код не выполнится, если была ошибка выше.")
```

---

## 🛑 Как проигнорировать отлов ошибок?
WhyCrash анализирует только **необработанные** (unhandled) исключения. Если вы хотите, чтобы ошибка в коде **не** попадала в WhyCrash и скрипт продолжал работать, используйте банальный блок `try...except`:

```python
import WhyCrash
WhyCrash.debug()

try:
    int("буква")
except ValueError:
    print("Ошибка перехвачена, до WhyCrash она не дойдет. Идем дальше!")
```

## ⚙️ Технологии под капотом
- **OpenRouter & Minimax** — отвечает за анализ кода, "Чтение мыслей" (Reasoning), и генерацию файлов с исправлением.
- **Traceback Walking** — скрипт автоматически идет по цепочке ошибки, находить все ваши `.py` файлы, которые задействованы, читает их и отправляет нейросети как контекст.
- **Rich** — красивый UI в консоли (цвета, панели, форматирование Markdown).

---

Сделано с ❤️ для сохранения нервов разработчиков!

---
🌍 **Языки:** [English](../README.md) | [Deutsch](README_de.md)
